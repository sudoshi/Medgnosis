# =============================================================================
# Medgnosis — Production Environment Configuration
# Used by systemd services (medgnosis-api, medgnosis-worker)
# =============================================================================

# Database
DATABASE_URL=postgres://smudoshi:acumenus@localhost:5432/medgnosis

# Redis (BullMQ + WebSocket pub/sub)
REDIS_URL=redis://localhost:6379

# API Server
API_PORT=3081
API_HOST=127.0.0.1
NODE_ENV=production
CORS_ORIGIN=https://medgnosis.acumenus.net

# Auth (JWT)
JWT_SECRET=o5im2zyDX9k9vBmnaEPCiXdPOML2slKyS4h9nf55reokK9b4p+IZVo8D8ZL5nPZD
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# AI Provider: 'anthropic' (cloud, requires BAA) or 'ollama' (local, no BAA)
AI_PROVIDER=ollama
AI_INSIGHTS_ENABLED=false

# Anthropic (only used if AI_PROVIDER=anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_BAA_SIGNED=false

# Ollama (local inference — no BAA required)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gemma:7b

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM=alerts@medgnosis.app

# Web App URL (for email links)
WEB_APP_URL=https://medgnosis.acumenus.net

# Observability
SENTRY_DSN=
