name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build all packages
        run: npx turbo run build

      # Add deployment steps here based on your hosting provider
      # Examples: Docker build + push, Vercel deploy, Railway, etc.
      - name: Build Docker images
        run: |
          echo "Docker build step — configure for your deployment target"
          # docker build -f apps/api/Dockerfile -t medgnosis-api .
          # docker build -f apps/web/Dockerfile -t medgnosis-web .
